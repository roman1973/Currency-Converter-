import React from 'react';

import styles from './home.module.scss';

const Home: React.FC = () => (
  <main>
    <div className={styles.wrapper}>{'Enjoy currency conversion and rate !!!!!'}</div>
  </main>
);

export default Home;
